{
  "name": "stream-scraper",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": { 
  "start": "node server.js",
  "build": "npm install && npx puppeteer browsers install chrome",
  "dependencies": { "cors": "^2.8.5", "express": "^4.18.2", "puppeteer": "^22.0.0" }
}
